package edu.brown.cs.student.main.server.RecommenderAlgorithm;

/**
 * An enum representing the two different recommendation modes possible in generating a MealPlan.
 */
public enum Mode {
  PERSONALIZED, MINIMIZE_FOOD_WASTE;
}
